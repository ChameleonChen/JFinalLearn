package test.myjfinal.core;

import java.util.Iterator;
import java.util.Set;

import junit.framework.TestCase;



public class TestActionMapping extends TestCase {

	/*
	//success
	public void testBuildExcludedMethodName() {
		ActionMapping mapping = new ActionMapping(null, null);
		Set<String> excludedMethodName = mapping.buildExcludedMethodName();
		Iterator<String> it = excludedMethodName.iterator();
		for (;it.hasNext();) {
			String methodName = it.next();
			System.out.println(methodName);
		}
	}
	*/
}
